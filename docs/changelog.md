# Changelog

<!-- Included from the repository root rather than copied: a changelog with two versions is
     a changelog with a wrong one, and the root file is what a clone and a release page
     show. `pymdownx.snippets` resolves the path against `base_path` (see mkdocs.yml). -->

--8<-- "CHANGELOG.md"
